services:
  - type: web
    name: autohunt-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm ci && npm run render:build
    startCommand: npm start
    healthCheckPath: /api/health/live
    envVars:
      - key: NODE_ENV
        value: production
